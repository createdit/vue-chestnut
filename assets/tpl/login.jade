style.
    body {
        background-color: #DADADA;
    }
    body > .grid {
        height: 100%;
    }
    .column {
        max-width: 450px;
    }
.ui.middle.aligned.center.aligned.grid
    .column
        h2.ui.teal.header
            .content 验证你的身份
        form.ui.large.form(v-bind:class="{'error': loginError}")
            .ui.stacked.segment
                .field
                    .ui.left.icon.input
                        .user.icon
                        input(type="text" placeholder="邮箱" v-model="email")
                .field
                    .ui.left.icon.input
                        .lock.icon
                        input(type="password" placeholder="密码" v-model="password")
                .ui.fluid.large.teal.submit.button(v-on:click="login") 登录
            .ui.error.message 账号或密码有误
